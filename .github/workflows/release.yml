name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Resolve package dependencies
        run: xcodebuild -resolvePackageDependencies -project Stuped.xcodeproj -scheme Stuped

      - name: Build
        run: |
          xcodebuild \
            -project Stuped.xcodeproj \
            -scheme Stuped \
            -configuration Release \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: Sign (ad-hoc)
        run: codesign --deep --force --sign - build/Build/Products/Release/Stuped.app

      - name: Package app
        run: ditto -c -k --keepParent build/Build/Products/Release/Stuped.app Stuped.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: Stuped.zip
          generate_release_notes: true
